/*Write a program to define class SHUFFLE_STRING with one data member namely name. Write
constructor to initialize name. Class have member function Show and Encode. Show function will display
name, and Encode function that takes two STRING type object as argument, and do the following.
Input: string1=”BMIIT” and string2=“ DCST”
Output: “BDMCISITT*/

import java.util.*;
class SHUFFLE_STRING
{
    String name1 , name2;
    SHUFFLE_STRING(String s1,String s2)
    {
        name1=name2=null;
    }
    void getdata()
    {
        System.out.println("enter name1");
        name1=sc.next();
        System.out.println("enter name2");
        name2=sc.next();
    }
    public static  void main(String args[])
    {
       SHUFFLE_STRING s =new SHUFFLE_STRING();
        for(int i=0;i<(s1.length))
    }
    
}